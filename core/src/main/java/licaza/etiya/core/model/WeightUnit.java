package licaza.etiya.core.model;

// Per set, since one gym mixes machines in kilos and in pounds.
// NONE is for sets without a weight, like pull-ups: the set is only a count
public enum WeightUnit {
  KG,
  LB,
  NONE
}
